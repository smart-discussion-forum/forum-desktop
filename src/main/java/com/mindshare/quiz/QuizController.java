package com.mindshare.quiz;

/**
 * Compatibility shim for older FXML or cached build artifacts that still
 * reference the legacy QuizController class name.
 */
public class    QuizController extends QuizConfigurationController {
}
